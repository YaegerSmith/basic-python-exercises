"""
 * Escribe un programa que muestre por consola (con un print) los
 * números de 1 a 100 (ambos incluidos y con un salto de línea entre
 * cada impresión), sustituyendo los siguientes:
 * - Múltiplos de 3 por la palabra "fizz".
 * - Múltiplos de 5 por la palabra "buzz".
 * - Múltiplos de 3 y de 5 a la vez por la palabra "fizzbuzz".
 """

def fizzbuzz():

    # Iteramos sobre los números del 1 al 100 (ambos incluidos).
    for num in range(1, 101):
    # Verificamos si el número es múltiplo de 3 y de 5.
        if num % 3 == 0 and num % 5 == 0:
        # Si es así, imprimimos "fizzbuzz"
            print("Fizzbuzz.")
    # Si no es múltiplo de 3 y de 5, verificamos si es múltiplo solo de 3
        elif num % 3 == 0:
        # Si es así, imprimimos "fizz"
            print("fizz")
    # Si no es múltiplo de 3, verificamos si es múltiplo solo de 5
        elif num % 5 == 0:
        # Si es así, imprimimos "buzz"
            print("buzz")
    # Si no es múltiplo de 3 ni de 5, imprimimos el número
        else:
            print(num)

fizzbuzz()
